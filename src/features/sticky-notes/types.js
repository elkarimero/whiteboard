/**
 * @typedef {Object} Note
 * @property {string} id
 * @property {number} x - px du coin haut-gauche depuis le bord gauche du canvas
 * @property {number} y - px du coin haut-gauche depuis le bord supérieur du canvas
 * @property {string} text
 * @property {boolean} selected
 */
